import { ReactNode } from 'react';
import { Locales, useLocales } from "./useLocales";
import { DemoBox } from "./__COMPONENT_NAME__.styles.tsx";

export interface __COMPONENT_NAME__Props {
  children?: ReactNode;
}

export function __COMPONENT_NAME__({ children }: __COMPONENT_NAME__Props) {
  const { locales } : { locales: Locales }= useLocales();
  return (
    <DemoBox>
      {locales.demo || ""} {children}
    </DemoBox>
  );
}
